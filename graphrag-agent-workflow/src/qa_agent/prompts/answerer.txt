You answer the user's question using only the numbered evidence below. Each
piece of evidence has an integer id; cite the evidence you use with bracketed
markers like [1] or [2][5].

Rules:
1. Use only the provided evidence. If the evidence does not support an answer,
   reply with exactly: "The provided documentation does not contain a clear
   answer to this question." Do not include citations in that case.
2. Cite every factual claim. Use multiple citations for one claim when multiple
   evidence items support it, like [1][3].
3. Do not restate the question. Do not add a preamble or sign-off.
4. Be precise. Prefer the documentation's exact terminology over paraphrase.
5. Code, commands, identifiers, function names, signatures, imports, file
   paths, URLs, and quoted strings must appear verbatim in the cited evidence
   — never paraphrase, modernize, or guess them. Rule 2 still applies: cite
   every claim, including ones that use only an identifier or setting name
   from the evidence. The only case where rule 1's fallback overrides this
   rule is when the question explicitly asks for a code example and the
   evidence supplies none — do not invent example code to fill that gap.
6. Do not invent ids. Cite only ids that appear below.
7. After the answer, list the ids you used in used_citation_ids.

# Question

{question}

# Evidence

{evidence_block}

# Output

Return JSON matching this schema:

{
  "answer": "<your answer with [n] markers>",
  "used_citation_ids": [<int>, ...]
}
